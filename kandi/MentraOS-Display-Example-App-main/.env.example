# MentraOS app identity
# Must match your app registration in https://console.mentra.glass
PACKAGE_NAME=com.yourname.rightsnow
MENTRAOS_API_KEY=your_mentraos_api_key_here
PORT=3000
# Public ngrok URL for iPhone companion requests
# Example: https://unlicentiated-unsqueamishly-mahalia.ngrok-free.app
NGROK_URL=https://unlicentiated-unsqueamishly-mahalia.ngrok-free.app

# Festival Assistant behavior
DEFAULT_LOCALE=en-US

# iPhone companion app should POST JSON payloads to:
#   {NGROK_URL}/api/overlay/hearing
#   {NGROK_URL}/api/overlay/friends
# with a valid Mentra `sessionId` field.

# Song ID fallback config (Gemini first -> Shazam fallback)
# Shazam via RapidAPI
SHAZAM_API_KEY=
SHAZAM_API_HOST=shazam-core.p.rapidapi.com
AUDD_API_KEY=109d5f65d3be41f8109a7e02ea84e180
# Optional logging: info | debug
LOG_LEVEL=info

# WORKAROUND: SDK display truncation fix
# Some versions of @mentra/sdk truncate ViewType.MAIN to 4 chars per line.
# Use 'PINNED' or 'DEFAULT' to avoid this. Options: MAIN | PINNED | DEFAULT
MENTRA_VIEW_TYPE=PINNED
